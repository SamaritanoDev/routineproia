name: routineproia
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  file_picker: ^8.1.4
  flutter:
    sdk: flutter
  google_generative_ai: ^0.4.6

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/undraw_fitness_tracker.png
    - assets/undraw_personal_training.png
    - assets/undraw_Working_out_re_nhkg.png
    - assets/logo-routinepro-100.png
